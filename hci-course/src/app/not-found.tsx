import NotFoundUI from "./components/UI/NotFoundUI";
// This one is default not-found page (not for those Links inside footer).
export default function NotFound() {
    return (
        <NotFoundUI />
    );
}