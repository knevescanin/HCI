import NotFoundUI from "../components/UI/NotFoundUI";
// This one is for those Links inside footer, that currently lead to nothing. 
export default function NotFound() {
    return (
        <NotFoundUI />
    );
}